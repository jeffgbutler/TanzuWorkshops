apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sslvolume
  namespace: openemr
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: tanzu-gold-storage-policy
  resources:
    requests:
      storage: 1Gi
